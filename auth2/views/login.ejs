<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-500 text-white p-10">
        <h1 class="mb-3">Login your account</h1>
<form method="post" action="/login">
    <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800"outline-none type="text" name="email"  placeholder="type email">
    <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800"outline-none type="text"name="password"placeholder="write password">
    <input class="bg-blue-500 px-2 py-2 rounded-md"outline-none type="submit" value="Login">
</form>
    </div>
</body>
</html> -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-600 flex items-center justify-center min-h-screen">

    <div class="w-full max-w-md bg-zinc-700 text-white p-8 rounded-xl shadow-lg">
        <h1 class="text-3xl font-bold mb-6 text-center">Login to Your Account</h1>

        <form method="post" action="/login" class="space-y-4">
            <input 
                class="w-full px-4 py-2 rounded-md bg-zinc-800 border-2 border-zinc-600 focus:border-blue-500 focus:outline-none transition-colors" 
                type="email" 
                name="email"  
                placeholder="Enter your email">

            <input 
                class="w-full px-4 py-2 rounded-md bg-zinc-800 border-2 border-zinc-600 focus:border-blue-500 focus:outline-none transition-colors" 
                type="password"
                name="password"
                placeholder="Enter your password">

            <input 
                class="w-full bg-blue-500 hover:bg-blue-600 transition-colors px-4 py-2 rounded-md font-semibold cursor-pointer" 
                type="submit" 
                value="Login">
        </form>

        <p class="mt-4 text-center text-gray-400 text-sm">
            Don't have an account? <a href="/" class="text-blue-400 hover:underline">Sign up</a>
        </p>
    </div>

</body>
</html>

